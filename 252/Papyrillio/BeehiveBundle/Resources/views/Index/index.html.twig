{{ index.type }} ({{ index.topic }}): <span id="index_{{ index.id }}_phrase" class="editIndex greek">{{ index.phrase }}</span><span id="delete_index_{{ index.id }}" class="action deleteIndex">Indexeintrag löschen</span>
{% extends 'base.html.twig' %}

{% block body %}

<h3>Passwort ändern</h3>
<form id="user" class="new" action="{{ path('PapyrillioUserBundle_password') }}" method="post" {{ form_enctype(form) }}>
    {{ form_errors(form) }}

    <div class="password">
      {{ form_label(form.password) }}
      {{ form_widget(form.password, {'value': ''}) }}
      {{ form_errors(form.password) }}
    </div>
    
    {{ form_widget(form) }}

    <input class="submit" type="submit" name="new" value="Neues Passwort setzen" />
</form>

{% endblock %}

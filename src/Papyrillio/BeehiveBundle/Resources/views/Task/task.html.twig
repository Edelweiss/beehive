{{ task.title }}: <span id="task_{{ task.id }}_description" class="editTask greek">{{ task.description }}</span> <span id="task_{{ task.id }}_cleared" class="editTaskSelect highlight">{{ task.isCleared ? '✔' : '✘' }}</span>